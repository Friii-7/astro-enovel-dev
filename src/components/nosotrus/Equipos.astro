---
// Componente Equipos con diseño de dos columnas como en la imagen
---

<section class="equipos-section">
  <div class="equipos-header">
    <h2 class="equipos-title">Equipos</h2>
    <p class="equipos-description">
      Automatiza procesos en Microsoft 365, protege tu información con Acronis y
      mejora tu productividad con Inteligencia Artificial.
    </p>
  </div>

  <div class="equipos-content">
    <div class="equipos-left">
      <div class="image-container">
        <img
          src="/images/Rectangle 54.png"
          alt="Equipo de trabajo"
          class="team-image"
        />
        <div class="ai-tag">Inteligencia artificial</div>
      </div>
    </div>

    <div class="equipos-right">
      <div class="content-box">
        <p class="ai-text">
          Impulsa tu trabajo con IA y haz más en menos tiempo.
        </p>
        <button class="ai-button">Ver más sobre IA</button>
      </div>
    </div>
  </div>

  <!-- Sección de Servicios -->
  <div class="servicios-container">
    <div class="servicios-grid">
      <!-- Card 1: SharePoint -->
      <div class="servicio-card">
        <div class="card-top">
          <div class="tag">SharePoint</div>
          <div class="illustration">
            <img
              src="/images/Group 15.png"
              alt="SharePoint - Organización y colaboración"
            />
          </div>
        </div>
        <div class="card-bottom">
          <p class="servicio-descripcion">
            Organiza, colabora y automatiza desde un solo lugar con SharePoint.
          </p>
          <button class="btn-ver-mas">Ver más</button>
        </div>
      </div>

      <!-- Card 2: Acronis -->
      <div class="servicio-card">
        <div class="card-top">
          <div class="tag">Acronis</div>
          <div class="illustration">
            <img
              src="/images/Rectangle 48.png"
              alt="Acronis - Protección de datos"
            />
          </div>
        </div>
        <div class="card-bottom">
          <p class="servicio-descripcion">
            Protege tu información con copias de seguridad inteligentes y
            seguras.
          </p>
          <button class="btn-ver-mas">Ver más</button>
        </div>
      </div>

      <!-- Card 3: Microsoft 365 -->
      <div class="servicio-card">
        <div class="card-top">
          <div class="tag">Microsoft 365</div>
          <div class="illustration">
            <img
              src="/images/Rectangle.png"
              alt="Microsoft 365 - Productividad y automatización"
            />
          </div>
        </div>
        <div class="card-bottom">
          <p class="servicio-descripcion">
            Automatiza tareas y mejora la productividad de tu equipo.
          </p>
          <button class="btn-ver-mas">Ver más</button>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .equipos-section {
    background: linear-gradient(
        rgba(255, 255, 255, 0.95),
        rgba(255, 255, 255, 0.95)
      ),
      url("/images/imageenovel1.png");
    background-size: cover;
    background-position: center;
    padding: 6rem 2rem;
    position: relative;
    max-width: 100vw;
    overflow-x: hidden;
  }

  .equipos-header {
    text-align: center;
    margin-bottom: 4rem;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  .equipos-title {
    font-size: 3.5rem;
    font-weight: 700;
    color: #010738;
    margin-bottom: 1.5rem;
    letter-spacing: -2px;
  }

  .equipos-description {
    font-family: Inter;
    font-size: 18px;
    font-weight: 400;
    font-style: normal;
    line-height: 28px;
    letter-spacing: 0%;
    color: #010738;
    white-space: nowrap;
    text-align: center;
    max-width: 600px;
    margin: 49px auto;
    /* padding: 0px 0rem; */
    margin-left: -152px;
  }

  /* Responsive para tablets y móviles */
  @media screen and (max-width: 1024px) {
    .equipos-section {
      padding: 4rem 1.5rem;
    }

    .equipos-title {
      font-size: 3rem;
      margin-bottom: 1.2rem;
    }

    .equipos-description {
      white-space: normal;
      margin: 25px auto;
      margin-left: auto;
      margin-right: auto;
      padding: 0 1rem;
      line-height: 1.6;
      text-align: center;
      max-width: 90%;
      font-size: 17px;
    }

    .equipos-content {
      max-width: 100%;
      height: auto;
      min-height: 160px;
      overflow: visible;
    }

    .image-container {
      overflow: visible;
      position: relative;
    }

    .content-box {
      padding: 1.5rem 2rem;
      flex-direction: row;
      gap: 1rem;
    }

    .ai-text {
      font-size: 1.1rem;
      max-width: 100%;
      width: auto;
      height: auto;
      padding: 1rem 0;
    }

    .ai-button {
      padding: 1rem 1.5rem;
      font-size: 0.95rem;
      white-space: nowrap;
    }

    .ai-tag {
      overflow: visible;
      white-space: nowrap;
      z-index: 10;
    }
  }

  /* iPad específicamente */
  @media screen and (min-width: 768px) and (max-width: 1024px) {
    .equipos-section {
      padding: 3.5rem 2rem;
    }

    .equipos-title {
      font-size: 2.8rem;
    }

    .equipos-description {
      white-space: normal;
      margin: 20px auto;
      margin-left: auto;
      margin-right: auto;
      padding: 0 1.5rem;
      line-height: 1.6;
      text-align: center;
      max-width: 85%;
      font-size: 16px;
    }

    .equipos-content {
      height: 140px;
      max-width: 100%;
      overflow: visible;
    }

    .image-container {
      height: 100%;
      overflow: visible;
      position: relative;
    }

    .content-box {
      padding: 1.2rem 1.8rem;
    }

    .ai-text {
      font-size: 1rem;
      padding: 0.8rem 0;
    }

    .ai-button {
      padding: 0.8rem 1.2rem;
      font-size: 0.9rem;
    }

    .ai-tag {
      top: 15px;
      left: 15px;
      padding: 6px 12px;
      font-size: 0.85rem;
      z-index: 10;
      position: absolute;
      white-space: nowrap;
      overflow: visible;
    }
  }

.equipos-content {
  max-width: 1197px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0;
  align-items: stretch;
  height: 187px;
  border-radius: 4px;
  overflow: hidden;
}

.equipos-left {
  position: relative;
}

.image-container {
  position: relative;
  height: 100%;
  border-radius: 0;
  overflow: hidden;
  box-shadow: none;
  display: flex;
  align-items: center;
}

.team-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
  filter: brightness(0.8) contrast(1.2);
  margin: 0;
}

.ai-tag {
  position: absolute;
  top: 30px;
  left: 30px;
  background: #006fdd;
  color: white;
  padding: 10px 20px;
  border-radius: 4px;
  font-size: 1rem;
  font-weight: 600;
  box-shadow: 0 4px 12px rgba(0, 102, 204, 0.3);
  z-index: 10;
  white-space: nowrap;
  overflow: visible;
}

.equipos-right {
  display: flex;
  align-items: center;
}

.content-box {
  background: #010738;
  padding: 2rem 2.5rem;
  border-radius: 0;
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  box-shadow: none;
  gap: 8px;
}

.ai-text {
  color: white;
  font-size: 1.25rem;
  font-weight: 500;
  line-height: 1.4;
  margin: 0;
  flex: 1;
  max-width: 394px;
  width: 394px;
  height: 111px;
  display: flex;
  align-items: center;
  padding-top: 40px;
  padding-bottom: 40px;
}

.ai-button {
  background: #ff6b35;
  color: white;
  border: none;
  padding: 40px 26px;
  border-radius: 4px;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  margin-left: 8px;
  box-shadow: 0 4px 15px rgba(255, 107, 53, 0.3);
  white-space: nowrap;
  gap: 8px;
}

.ai-button:hover {
  background: #e55a2b;
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(255, 107, 53, 0.4);
}

/* Estilos para la sección de Servicios */
.servicios-container {
  max-width: 1200px;
  margin: 4rem auto 0;
  padding: 0 20px;
}

.servicios-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
  gap: 30px;
  margin-top: 40px;
}

.servicio-card {
  background: transparent;
  border-radius: 4px;
  overflow: hidden;
  transition: all 0.3s ease;
  height: 500px;
  display: flex;
  flex-direction: column;
}

.servicio-card:hover {
}

.card-top {
  background: transparent;
  padding: 0;
  flex: 1;
  display: flex;
  flex-direction: column;
  position: relative;
}

.tag {
  background: #007bff;
  color: white;
  padding: 8px 16px;
  border-radius: 4px;
  font-size: 14px;
  font-weight: 600;
  position: absolute;
  top: 20px;
  left: 20px;
  z-index: 10;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.illustration {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
}

.illustration img {
  width: 100%;
  height: 101%;
  object-fit: cover;
  border-radius: 16px 16px 0 0;
}

.card-bottom {
  background: #010738;
  color: white;
  padding: 30px;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.servicio-descripcion {
  font-size: 16px;
  line-height: 1.6;
  margin-bottom: 25px;
  font-weight: 400;
  color: white;
}

.btn-ver-mas {
  background: #fa4517;
  color: white;
  border: none;
  width: 298.43px;
  height: 37.76px;
  border-radius: 4px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.btn-ver-mas:hover {
  background: #fa4517;
  transform: translateY(-2px);
  box-shadow: 0 5px 15px rgba(255, 69, 0, 0.3);
}

  @media screen and (max-width: 768px) {
    .equipos-section {
      padding: 3rem 1rem;
    }

    .equipos-title {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }

    .equipos-description {
      font-size: 15px;
      margin: 18px auto;
      margin-left: auto;
      margin-right: auto;
      padding: 0 0.5rem;
      line-height: 1.5;
      text-align: center;
      max-width: 95%;
    }

    .equipos-content {
      grid-template-columns: 1fr;
      height: auto;
      min-height: 200px;
    }

    .image-container {
      height: 120px;
    }

    .content-box {
      padding: 1.5rem;
      flex-direction: column;
      text-align: center;
      gap: 1rem;
    }

    .ai-text {
      font-size: 1.1rem;
      margin-bottom: 0;
      flex: none;
      max-width: 100%;
      width: auto;
      height: auto;
      padding: 0;
    }

    .ai-button {
      padding: 12px 24px;
      font-size: 0.95rem;
      margin-left: 0;
      width: auto;
      min-width: 200px;
    }

    .ai-tag {
      top: 15px;
      left: 15px;
      padding: 6px 12px;
      font-size: 0.85rem;
    }
  }

  @media screen and (max-width: 480px) {
    .equipos-section {
      padding: 2.5rem 0.8rem;
    }

    .equipos-title {
      font-size: 2.2rem;
      margin-bottom: 0.8rem;
    }

    .equipos-description {
      font-size: 14px;
      margin: 15px auto;
      padding: 0 0.3rem;
      line-height: 1.4;
    }

    .equipos-content {
      min-height: 180px;
    }

    .image-container {
      height: 100px;
    }

    .content-box {
      padding: 1.2rem 1rem;
      gap: 0.8rem;
    }

    .ai-text {
      font-size: 1rem;
      margin-bottom: 0;
    }

    .ai-button {
      padding: 10px 20px;
      font-size: 0.9rem;
      min-width: 180px;
    }

    .ai-tag {
      top: 10px;
      left: 10px;
      padding: 5px 10px;
      font-size: 0.8rem;
    }
  }

  /* Responsive para servicios - iPad */
  @media screen and (min-width: 481px) and (max-width: 768px) {
    .servicios-container {
      padding: 0 25px;
      margin: 3rem auto 0;
    }

    .servicios-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 22px;
      margin-top: 30px;
    }

    .servicio-card {
      height: 440px;
      border-radius: 4px;
    }

    .card-top {
      padding: 0;
    }

    .illustration img {
      border-radius: 4px 4px 0 0;
    }

    .tag {
      top: 12px;
      left: 12px;
      font-size: 11px;
      padding: 5px 10px;
    }

    .card-bottom {
      padding: 22px 18px;
    }

    .servicio-descripcion {
      font-size: 14px;
      margin-bottom: 18px;
      line-height: 1.4;
    }

    .btn-ver-mas {
      width: 100%;
      max-width: 210px;
      height: 37px;
      font-size: 12px;
    }
  }

  /* Responsive Design para servicios */
  /* iPad y tablets */
  @media screen and (min-width: 768px) and (max-width: 1024px) {
    .servicios-container {
      padding: 0 20px;
      margin: 4rem auto 0;
      max-width: 100%;
    }

    .servicios-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 15px;
      margin-top: 30px;
    }

    .servicio-card {
      height: 400px;
      border-radius: 4px;
      min-width: 0;
    }

    .card-top {
      padding: 0;
      min-height: 160px;
    }

    .illustration {
      min-height: 160px;
    }

    .illustration img {
      border-radius: 8px 8px 0 0;
      height: 100%;
      min-height: 160px;
      object-fit: cover;
    }

    .tag {
      top: 10px;
      left: 10px;
      font-size: 10px;
      padding: 4px 8px;
      border-radius: 4px;
      font-weight: 600;
    }

    .card-bottom {
      padding: 15px 12px;
      border-radius: 0 0 8px 8px;
    }

    .servicio-descripcion {
      font-size: 13px;
      margin-bottom: 15px;
      line-height: 1.3;
    }

    .btn-ver-mas {
      width: 100%;
      max-width: 140px;
      height: 32px;
      font-size: 11px;
      border-radius: 4px;
      padding: 0;
    }
  }

  /* iPad Pro específicamente */
  @media screen and (min-width: 1024px) and (max-width: 1366px) {
    .servicios-container {
      padding: 0 30px;
      margin: 4rem auto 0;
    }

    .servicios-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 20px;
      margin-top: 35px;
    }

    .servicio-card {
      height: 420px;
      border-radius: 8px;
    }

    .card-top {
      padding: 0;
      min-height: 180px;
    }

    .illustration {
      min-height: 180px;
    }

    .illustration img {
      border-radius: 8px 8px 0 0;
      height: 100%;
      min-height: 180px;
    }

    .tag {
      top: 12px;
      left: 12px;
      font-size: 11px;
      padding: 5px 10px;
      border-radius: 4px;
    }

    .card-bottom {
      padding: 20px 15px;
      border-radius: 0 0 8px 8px;
    }

    .servicio-descripcion {
      font-size: 14px;
      margin-bottom: 18px;
      line-height: 1.4;
    }

    .btn-ver-mas {
      width: 100%;
      max-width: 160px;
      height: 36px;
      font-size: 12px;
      border-radius: 4px;
    }
  }

  @media screen and (max-width: 767px) {
    /* Responsive para servicios - iPad */
    .servicios-container {
      padding: 0 20px;
      margin: 3rem auto 0;
    }

    .servicios-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
      margin-top: 30px;
    }

    .servicio-card {
      height: 420px;
      border-radius: 4px;
    }

    .card-top {
      padding: 0;
    }

    .illustration img {
      border-radius: 4px 4px 0 0;
    }

    .tag {
      top: 12px;
      left: 12px;
      font-size: 11px;
      padding: 5px 10px;
    }

    .card-bottom {
      padding: 20px 15px;
    }

    .servicio-descripcion {
      font-size: 14px;
      margin-bottom: 18px;
      line-height: 1.4;
    }

    .btn-ver-mas {
      width: 100%;
      max-width: 200px;
      height: 36px;
      font-size: 12px;
    }
  }

  @media screen and (max-width: 480px) {
    /* Responsive para servicios - Mobile */
    .servicios-container {
      padding: 0 12px;
      margin: 2rem auto 0;
    }

    .servicios-grid {
      grid-template-columns: 1fr;
      gap: 16px;
      margin-top: 20px;
    }

    .servicio-card {
      height: auto;
      min-height: 360px;
      border-radius: 8px;
    }

    .servicio-card:hover {
    }

    .card-top {
      padding: 0;
      min-height: 200px;
    }

    .illustration {
      min-height: 200px;
    }

    .illustration img {
      border-radius: 8px 8px 0 0;
      height: 100%;
      min-height: 200px;
    }

    .tag {
      top: 12px;
      left: 12px;
      font-size: 11px;
      padding: 6px 12px;
      border-radius: 6px;
      font-weight: 700;
      letter-spacing: 0.3px;
    }

    .card-bottom {
      padding: 20px 16px;
      border-radius: 0 0 8px 8px;
    }

    .servicio-descripcion {
      font-size: 14px;
      margin-bottom: 18px;
      line-height: 1.4;
      font-weight: 400;
      color: #f8f9fa;
    }

    .btn-ver-mas {
      width: 100%;
      max-width: 200px;
      height: 40px;
      font-size: 12px;
      margin: 0 auto;
      border-radius: 6px;
      font-weight: 600;
      letter-spacing: 0.5px;
      text-transform: uppercase;
      transition: all 0.3s ease;
    }

    .btn-ver-mas:hover {
      background: #fa4517;
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(255, 69, 0, 0.4);
    }
  }

  /* Responsive para tablets medianas */
  @media screen and (min-width: 481px) and (max-width: 768px) {
    .servicios-container {
      padding: 0 25px;
      margin: 3rem auto 0;
    }

    .servicios-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 22px;
      margin-top: 30px;
    }

    .servicio-card {
      height: 440px;
      border-radius: 4px;
    }

    .card-top {
      padding: 0;
    }

    .illustration img {
      border-radius: 4px 4px 0 0;
    }

    .tag {
      top: 12px;
      left: 12px;
      font-size: 11px;
      padding: 5px 10px;
    }

    .card-bottom {
      padding: 22px 18px;
    }

    .servicio-descripcion {
      font-size: 14px;
      margin-bottom: 18px;
      line-height: 1.4;
    }

    .btn-ver-mas {
      width: 100%;
      max-width: 210px;
      height: 37px;
      font-size: 12px;
    }
  }

  /* Responsive para móviles muy pequeños */
  @media screen and (max-width: 360px) {
    .servicios-container {
      padding: 0 8px;
      margin: 1.5rem auto 0;
    }

    .servicios-grid {
      gap: 14px;
      margin-top: 18px;
    }

    .servicio-card {
      min-height: 340px;
      border-radius: 6px;
    }

    .card-top {
      min-height: 180px;
    }

    .illustration {
      min-height: 180px;
    }

    .illustration img {
      border-radius: 6px 6px 0 0;
      min-height: 180px;
    }

    .tag {
      top: 10px;
      left: 10px;
      font-size: 10px;
      padding: 5px 10px;
      border-radius: 4px;
    }

    .card-bottom {
      padding: 16px 12px;
      border-radius: 0 0 6px 6px;
    }

    .servicio-descripcion {
      font-size: 13px;
      margin-bottom: 16px;
      line-height: 1.3;
    }

    .btn-ver-mas {
      max-width: 180px;
      height: 36px;
      font-size: 11px;
      border-radius: 4px;
    }
  }
</style>
